# 给定的变量值
rho = 7.8e3  # 举例，你需要提供具体的值
rho_0 = 0.95e3  # 举例，你需要提供具体的值
g = 9.79  # 重力加速度，单位 m/s^2
d = 0.00103  # 举例，你需要提供具体的值
v_0 = 0.00688  # 举例，你需要提供具体的值
D = 0.02  # 举例，你需要提供具体的值

# 计算 eta 的值
eta = ((rho - rho_0) * g * d**2) / (18 * v_0 * (1 + 2.4 * d / D))

# 计算 eta_1 的值
eta_1 = eta - (3 / 16) * v_0 * d * rho_0

# 输出结果
print(f"eta 的值: {eta} Pa⋅s")
print(f"eta_1 的值: {eta_1} Pa⋅s")
